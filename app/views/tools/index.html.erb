<div class="card w-50 mx-auto mt-3">
  <ul class="list-group list-group-flush">
    <% @tools.each do |tool| %>
      <%= link_to tool.name, tool_path(tool), class: "list-group-item text-center p-5" %>
    <% end %>
  </ul>
</div>
